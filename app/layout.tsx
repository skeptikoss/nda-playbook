import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "NDA Review Playbook | Legal Analysis Tool",
  description: "Streamline NDA review with AI-powered clause analysis and party-perspective legal guidance",
  keywords: ["NDA", "legal", "contract review", "compliance", "Singapore"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}